apiVersion: v1
kind: Service
metadata:
  name: payment-switch-svc
spec:
  selector:
    app.kubernetes.io/name: payment-switch-svc
  ports:
    - name: server
      protocol: TCP
      port: 8080
      targetPort: 8080
