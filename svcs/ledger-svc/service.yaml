apiVersion: v1
kind: Service
metadata:
  name: ledger-svc
spec:
  selector:
    app.kubernetes.io/name: ledger-svc
  ports:
    - name: server
      protocol: TCP
      port: 8080
      targetPort: 8080