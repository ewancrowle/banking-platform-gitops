apiVersion: v1
kind: Service
metadata:
  name: identity-svc
spec:
  selector:
    app.kubernetes.io/name: identity-svc
  ports:
    - name: server
      protocol: TCP
      port: 8080
      targetPort: 8080