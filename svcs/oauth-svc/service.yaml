apiVersion: v1
kind: Service
metadata:
  name: oauth-svc
spec:
  selector:
    app.kubernetes.io/name: oauth-svc
  ports:
    - name: server
      protocol: TCP
      port: 8080
      targetPort: 8080