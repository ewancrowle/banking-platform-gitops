apiVersion: apps/v1
kind: Deployment
metadata:
  name: oauth-svc
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: oauth-svc
  replicas: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: oauth-svc
    spec:
      containers:
        - name: oauth-svc
          image: ghcr.io/ewancrowle/banking-platform-apps/oauth-svc:latest
          ports:
            - containerPort: 8080
              name: server
          env:
            - name: IDENTITY_SERVICE_ADDR
              value: identity-svc.platform.svc.cluster.local:8080
            - name: ACCOUNT_SERVICE_ADDR
              value: account-svc.platform.svc.cluster.local:8080
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: jwt-secret
            - name: DB_HOST
              value: postgresql.postgresql.svc.cluster.local:5432
            - name: DB_NAME
              value: banking_platform
            - name: DB_USER
              value: postgres
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgresql
                  key: postgres-password