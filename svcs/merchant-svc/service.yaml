apiVersion: v1
kind: Service
metadata:
  name: merchant-svc
spec:
  selector:
    app.kubernetes.io/name: merchant-svc
  ports:
    - name: server
      protocol: TCP
      port: 8080
      targetPort: 8080