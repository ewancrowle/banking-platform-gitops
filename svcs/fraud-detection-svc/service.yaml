apiVersion: v1
kind: Service
metadata:
  name: fraud-detection-svc
spec:
  selector:
    app.kubernetes.io/name: fraud-detection-svc
  ports:
    - name: server
      protocol: TCP
      port: 8080
      targetPort: 8080
