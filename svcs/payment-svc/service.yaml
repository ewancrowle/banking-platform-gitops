apiVersion: v1
kind: Service
metadata:
  name: payment-svc
spec:
  selector:
    app.kubernetes.io/name: payment-svc
  ports:
    - name: server
      protocol: TCP
      port: 8080
      targetPort: 8080