apiVersion: apps/v1
kind: Deployment
metadata:
  name: confirmation-of-payee-svc
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: confirmation-of-payee-svc
  replicas: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: confirmation-of-payee-svc
    spec:
      containers:
        - name: confirmation-of-payee-svc
          image: ghcr.io/ewancrowle/banking-platform-apps/confirmation-of-payee-svc:latest
          ports:
            - containerPort: 8080
              name: server
          env:
            - name: IDENTITY_SERVICE_ADDR
              value: http://identity-svc:8080
            - name: DB_HOST
              value: postgresql.postgresql.svc.cluster.local:5432
            - name: DB_NAME
              value: banking_platform
            - name: DB_USERNAME
              value: postgres
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgresql
                  key: postgres-password
