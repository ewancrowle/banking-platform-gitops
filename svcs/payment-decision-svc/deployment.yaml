apiVersion: apps/v1
kind: Deployment
metadata:
  name: payment-decision-svc
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: payment-decision-svc
  replicas: 1
  template:
    metadata:
      labels:
        app.kubernetes.io/name: payment-decision-svc
    spec:
      containers:
        - name: payment-decision-svc
          image: ghcr.io/ewancrowle/banking-platform-apps/payment-decision-svc:latest
          ports:
            - containerPort: 8080
              name: server
          env:
            - name: IDENTITY_SERVICE_ADDR
              value: http://identity-svc:8080
            - name: LEDGER_SERVICE_ADDR
              value: http://ledger-svc:8080
            - name: DB_HOST
              value: postgresql.postgresql.svc.cluster.local:5432
            - name: DB_NAME
              value: banking_platform
            - name: DB_USERNAME
              value: postgres
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgresql
                  key: postgres-password
            - name: FRAUD_DETECTION_SERVICE_ADDR
              value: http://fraud-detection-svc:8080
